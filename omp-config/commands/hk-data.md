---
description: "Scaffold data layer: SQLite schema + CRUD helpers"
---

I need to store $ARGUMENTS.

Ask me: JS or Python?

Then:
1. Design the schema (tables, columns, types, indexes)
2. Create a data access module with CRUD functions
3. If JS: use better-sqlite3 (import Database from 'better-sqlite3')
4. If Python: use built-in sqlite3 module
5. Add the package to the project
6. Write a quick test script that inserts a sample record and reads it back
7. Tell me how to import and use the data module
